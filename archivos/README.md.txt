Ejercicio 1:
Aqui hago dos loops anidados, el primero del 1 al limite y el segundo todos los numeros menores que el indice del loop anterior. Veo si el índice del primer loop es divisible por el indice del segundo loop. Si lo es termino el ciclo. Si termina el segundo loop normalmente entoces ese numero es primo y lo añado a la lista de primos

Ejercicio 2: 
Aquí hago un decorado como una función que llama a otra como argumento. tengo dos funciones, raiz y cuadrado como pruebas. tomo el tiempo inicial inmediatamente antes de correr la funcion argumento y tomo el tiempo final inmediatamente despues del correr dicha función. Retorno la resta dichos tiempos. 

Ejercicio 3:

Aquí utilizo la librería intertools para implementar un contador que se incrementa cada vez que se crea una nueva instancia. Tengo dos clases una para metaclases

Ejercicio 4: 
Aqui tengo 3 archivos csv: Primercsv.csv, Segundocsv.csv y Tercercsv.csv. Utilizo la librería asyncio para correr la funcion función sumarvalores que diseñé con la librería pandas. Utilizo la instruccion o etiqueta await en la instrucción donde sumo los valores de la columna "value" de cada archivo csv

Ejercicio 5: 

Aquí escribí la funcion espalindroma que detecta si una palabra es o no un palintroma. Luego ulilizo Pytest para probar la ejecución de dicha función con las palabras murcielago y anilina.

Ejercicio 6:

aquí creo la clase conexion con 3 metodos conectar, correrconsulta y desconectar. Cree una base de datios PruebaDB vacía para hacer las pruebas que se incluyen en el código

Ejercicio 7:

Aquí utilizo la funcion Pool de la librería multiprocessing. creo además la función contar_palabras para contar el número de palabras en varios archivos de texto. finalmente utilizo la lista de los nombres de los archivos y la lista de los conteos para formar el dicionario que se pide en dicho ejercicio. Utilizo los archivos Gago.txt, Checo.txt y Jets.txt